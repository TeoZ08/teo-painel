export interface ContextProject { id: string; name: string; aliases: string[]; summary: string; currentSituation: string; officialStatus: 'active' | 'paused' | 'blocked' | 'completed' | 'archived' | 'unclassified'; stage: string | null; priority: 'high' | 'medium' | 'low' | null; canonicalNextAction: string | null; canonicalBlocker: string | null; tags: string[]; repositories: Array<{ name: string; role: string | null; trackActivity: boolean | null }>; reviewedAt: string | null; nextReviewAt: string | null; recentActivity: Array<{ occurredOn: string; description: string; source: string; type: string }>; decisions: Array<{ title: string; occurredOn: string | null; summary: string; source: string }>; source: { path: string; url: string | null; updatedAt: string | null } }
export interface TeoContextSnapshot { schemaVersion: 1; generatedAt: string; source: { repository: 'TeoZ08/teo-contexto'; ref: 'main'; commit: string }; projects: ContextProject[]; unmappedMentions: Array<{ source: string; description: string }>; warnings: Array<{ path: string; message: string }> }
